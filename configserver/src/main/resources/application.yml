server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/AndyLvVip/uca
          search-paths: config_repo/authserver,config_repo/eurekaserver,config_repo/gwserver,config_repo/zipkinservice,config_repo/authclient